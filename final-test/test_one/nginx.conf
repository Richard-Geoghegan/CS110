worker_processes 1;
events { worker_connections 1024; }
http {
    upstream app { server app-service:8000; }
    server {
        listen 80;
        location / {
            proxy_pass http://app;
            proxy_set_header Host $host;
        }
    }
}
